services:
  - type: web
    name: nin-frontend
    runtime: static
    buildCommand: npm run build
    staticPublishPath: dist/frontend/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
